# Журнал работы · rovensky.ru/pre-seed

Хроника совместной работы Михаила Ровенского и Claude (Anthropic) над инвестиционной страницей.
Ведётся Claude, дописывается после каждого значимого блока работы. Полные тексты аудитов — в `audit/pre-seed-2026-07/`. Детальная история правок — в описаниях pull request'ов репозитория.

---

## Контекст проекта

**Задача:** страница частного предложения займа — ₽1,5 млн под 15–20% годовых на 18 месяцев, под запуск студии AI-продуктов (сервисы: заказная разработка + SMM-подписка; флагманы: AURA, MyBrain; платформы 2027: MyUniverse, Meridian). Аудитория: частные инвесторы — владельцы бизнесов и топ-менеджеры из личного нетворка, 45+, финансово грамотные. Канал: личная ссылка после телефонного звонка.

**Инфраструктура:** GitHub Pages, домен rovensky.ru (переведён с Tilda в этой же работе), один HTML-файл без внешних зависимостей. Дизайн-система «бумага/чернила»: фон #F3EEE5, чернила #1B1A17, янтарь #E0A458, серифы Fraunces + моно IBM Plex Mono.

## Хроника (июль 2026)

### Домен и инфраструктура
- rovensky.ru переведён с Tilda на GitHub Pages (DNS, HTTPS, custom domain).
- Фавикон: сначала базовый, затем финальный — крупная серифная R (контур из шрифта) на чернильном квадрате; общий для всех страниц домена, `?v=3` для сброса кэша.

### Страница: от v1 к v2.x
- **v1** — первая версия-лонгрид (сохранена в `/pre-seed/v1/`). Пять параллельных ревьюеров (VC, частный инвестор, копирайтер, арт-директор, продуктовый стратег) потребовали: сократить вдвое, сделку — вперёд, честные статусы продуктов вместо «в эфире».
- **v2.1** — полная пересборка: 10 разделов, сделка первой, терминальная секция с интерактивным калькулятором займодавца (слайдер ставки, график накопленной выручки против долга), раскрывашки для глубины, форма. Второй раунд из 7 агентов (логика цифр, холодное чтение, арт-дирекшн + панель вау-эффектов с судьёй) → исправлены нестыковки цифр, добавлен скраб по графику (палец/курсор → значения месяца терминальной строкой).
- Продвинута на основной адрес `/pre-seed/`, старые URL редиректят.

### Надёжность формы
Во время теста лёг FormSubmit (Cloudflare 522). Итоговая схема: отправка **двумя независимыми каналами параллельно** (FormSubmit → почта; ntfy.sh → пуш + дубль на почту), успех = ответил любой; при отказе обоих — панель с готовым письмом (mailto) и Telegram; черновик полей живёт в localStorage и не теряется. Логика формы изолирована в отдельный скрипт (ошибки остальной страницы её не задевают), мгновенная видимая реакция на кнопку, видимый номер сборки в футере для отличения кэша.
**Открытый пункт:** одноразовая активация FormSubmit (первая доставка → клик по письму от сервиса) — на стороне Михаила. Подписка на ntfy-тему `rovensky-preseed-lead-k9x2m7q4vt` в приложении ntfy — по желанию.

### Большой аудит (протокол Михаила, 7 агентов + арбитр)
Фаза 0: рыночный фон июля 2026 (ключевая 14,25%; вклады 10–13%; ОФЗ 13,5–14,3%; корп. облигации A 17–19%; НДФЛ 13% с процентов) + вводные: питч голосом по телефону; ₽100 млн — устная договорённость; активов за поручительством нет; страница не разослана.
Фаза 1: семь разносов (партнёр YC, кредитный аналитик, юрист РФ, скептик юнит-экономики, редактор, UX, циничный инвестор) — `01-CRITIQUE.md`.
Фаза 2: арбитр, 34 позиции по корзинам A/B/C/D — `02-VERDICT.md`.
**Решение Михаила: запуск вместо бесконечной полировки** — применить необходимое на усмотрение Claude, продукты показывать открыто. Применено (сборка 2.2.0): честное «без залога и поручительства, отвечаю всем имуществом (ст. 24 ГК)» вместо юридически пустого «поручительства»; честный статус ₽100 млн («устная договорённость, письменное соглашение — до сделки»); строка «после НДФЛ» в калькуляторе; дисклеймер «не оферта», замена лексики инвестор/раунд → займодавец/займ; согласие на обработку данных; вариант Б как договор услуг. Отложено (см. `03-BACKLOG.md`): ставка-вилка, график выплат, статус ИП, страхование жизни, фото.
Фаза 5: речь к питчу — `05-SPEECH.md` (60 сек + 6 минут с ремарками + Q&A на 12 жёстких вопросов).

### Возврат амбиции (запрос Михаила: «не цепляет, нет инновационности»)
- Манифест после титульной: «Рынок полон инструментов… Мы строим системы, которые делают работу сами — процесс целиком»; цитаты Амодеи/Хуанга/Альтмана перенесены под него («— Это видят не только мы»).
- Тезисы категории на карточках: AURA «не CRM — замена процесса», MyBrain «не заметки — от мысли до результата», SMM Engine «не инструмент — вся редакция».
- Лестница «Ступень 1 → 2 → 3» с ролями в конструкции; блок с лидерами индустрии.
- **Интерактивная карта портфеля** (SVG-чертёж): три зоны времени, узлы на шине «технологическое ядро», стрелки эволюции SMM→Meridian и MyBrain→MyUniverse; прорисовка при скролле, тап по узлу → описание терминальной строкой.

### Тон и титульная (правки Михаила)
- Убраны «инструктажные» блоки (пошаговая механика сделки, «чем лучше вклада» как риторика) — тон равных для аудитории владельцев бизнеса; «в течение часа», «безналичный расчёт», «документы для комплаенса» — убраны.
- Вариант Б: гибкий зачёт — вся сумма или часть, сроки в договоре под состав работ.
- Сроки платформ: 2028+ → **2027** везде.
- Титульная как обложка частного меморандума: «Студия Михаила Ровенского · частное предложение» → заголовок **«Автономные системы»** → один абзац → две кнопки → одна моно-строка фактов. «Ответственность имуществом» с обложки убрана (осталась в 01/02).

### Скорость и доступность без VPN (июль 2026)
Жалоба: страница почти не открывается, особенно без VPN. Причина — блокирующая загрузка Google Fonts (fonts.googleapis.com), недоступного из РФ без VPN. Побочно выяснилось: **Fraunces не имеет кириллического набора вообще** — все русские заголовки рендерились подстановкой Georgia, а ~100 КБ шрифта грузились впустую.
Решение: серифом стал **Playfair Display** (настоящая кириллица, тот же издательский контраст), все шрифты перенесены в репозиторий (`/fonts`, latin+cyrillic, 195 КБ, кэшируются); Inter заменён на системный шрифт (0 КБ); удалены все preconnect и внешние стили. Обе страницы делают **ноль внешних запросов** при загрузке; архивная v1 тоже отвязана. Проверено с полностью заблокированной внешней сетью: загрузка 0,4 с, вёрстка и интерактив целы.

### Доступность из РФ (август 2026) — РЕШЕНО
Симптом: страница не открывается с телефона (мобильный интернет), медленно с десктопа. Диагностика: домен rovensky.ru проксируется через **Cloudflare** (A-записи 185.199.108–111.153 верные, но со статусом Proxied) — Cloudflare в РФ у мобильных операторов деградирует. Проверка прямого адреса `x79628848009-dot.github.io` — **тоже не открывается**, то есть оператор режет и сам GitHub Pages.
**Решение:** в Cloudflare все записи `rovensky.ru` (4×A) и `www` переключены с «Proxied» на «DNS only» — Cloudflare остался только DNS-провайдером, трафик идёт напрямую на GitHub Pages (185.199.108–111.153). GitHub выпустил свой сертификат, «DNS check successful», Enforce HTTPS включён. **Проверено с мобильного интернета — страница открывается.**
Почему это сработало, хотя `github.io` у оператора не открывался: фильтрация идёт по имени домена в запросе, а не по IP. Обращение к тем же серверам под именем `rovensky.ru` фильтр пропускает.
Дополнительно как страховка: **автономная сборка** `offline/rovensky-pre-seed.html` — один файл 369 КБ со встроенными шрифтами и иконками, ноль внешних запросов, открывается без интернета; можно отправлять инвесторам файлом в Telegram/почтой.
**На будущее:** не включать проксирование Cloudflare для этого домена. У `mybrain.rovensky.ru` статус Proxied сохранён и он ведёт на Vercel — этот адрес из РФ, вероятно, недоступен; разобрать отдельно.

### Финальная кода «Одно созвездие» (август 2026)
Завершающий экран в тёмной секции контактов: созвездие из четырёх янтарных звёзд (AURA, Meridian, MyUniverse, MyBrain), соединённых в одну фигуру, над полем мерцающих фоновых звёзд; линии прорисовываются при скролле. Взаимосвязь — самой композицией плюс строка «одно ядро, общие данные, общая механика». Скрытое приглашение: пунктирные нити сходятся к пятой неподписанной точке; текст сеет интригу («Пятая точка на карте пока не подписана»), тап по пустому узлу отвечает: «место следующей системы студии — вариант Б делает заказчика соавтором этой карты». Тап по любой звезде — описание терминальной строкой. Автономная сборка обновлена.

### Затор публикации GitHub Pages (6 августа 2026)
После пересоздания custom domain в настройках Pages (Remove → добавить заново) очередь деплоя заклинило: сборка проходит успешно, а шаг публикации висит в `deployment_queued` и падает по таймауту. Запуск #34 застрял в статусе queued и, судя по поведению, держит блокировку окружения `github-pages`; GitHub отказывается отменять его через API («Cannot cancel a workflow re-run that has not yet queued»). Запуски #35 и #36 (с созвездием) по той же причине не публикуются — сайт отдаёт предыдущую версию.
**Лечится вручную:** Actions → застрявший запуск → Cancel workflow; либо Settings → Pages → Unpublish site → заново выбрать Source (Deploy from a branch · main · /root) → Save.

### Новая версия: работа в черновике (август 2026)
Решение Михаила: боевая версия остаётся на `rovensky.ru/pre-seed/` нетронутой, новая версия разрабатывается параллельно в отдельном черновике **`rovensky.ru/next/`** (копия боевой на момент старта, сборка 3.0.0-draft). У черновика в углу красная плашка «Черновик · не для отправки» (`#draft-badge`) — снять при публикации. Публикация новой версии = копирование `next/index.html` → `pre-seed/index.html` после решения Михаила.
Схема работы (решение Михаила): **веб-версия Claude — обсуждение** (читает репозиторий, ничего не коммитит), **Claude Code — исполнение** (все правки, проверка, публикация). Правила зафиксированы в `docs/collaboration.md`. Для контекста подготовлен самодостаточный бриф `docs/context-brief.md` — сделка, портфель, цифры плана, рыночный фон, выработанные принципы (честность, тон равных, юридическая точность, амбиция по Тилю), техограничения (ноль внешних запросов, РФ без VPN), дизайн-система, структура и открытые вопросы.

### Полировка лаборатории по документу веб-Claude (август 2026)
Первый полный цикл схемы «веб-Claude обсуждает → Михаил передаёт документ → Claude Code вносит». Применено: регистр технологической компании вместо канцелярии — пять систем по функции (Инженерия · Исследования · Редакция · Сделки · Операции), имена продуктов ушли в моно-подписи; заголовок «Один человек. Остальное — системы»; eyebrow «Как устроена студия»; новая подводка («компания нового формата… две системы уже работают, третья строится»); отзвуки в манифесте («Начали с себя…») и созвездии («Собирает их одна лаборатория…»). Отклонение от документа: «займ не оплачивает офис и зарплаты» заменено на «офис и команду» — «зарплат нет» противоречило бы строке сметы «Работа основателя ₽500k» (принцип честности). Ширины подписей выверены по getBBox, применены короткие варианты из фолбэков документа.

### Стратегический бриф для новой версии (24 августа 2026)
Собран `docs/strategy-brief-2026-08.md` — самодостаточный документ с актуальной позицией по проектам, для обсуждения новой версии страницы. Источник: стратегический репозиторий `mybrain`, снимок на коммит `ba77214`; прочитан целиком, включая вложенные папки.
**Зачем:** веб-версия Claude читает только `pre-seed` и не видит стратегический репозиторий, а позиция с июля изменилась.
**Главное в брифе:** ИИ-конвейер MyBrain работает в проде с 9 августа (на бесплатном тире, не за деньги) — довод «деньги нужны, чтобы включить мозги» больше не работает; продукт стал многопользовательским, что было последним техническим блокером беты; бета переопределена как 40 мест по 990 ₽/мес навсегда; появился арифметический потолок бесплатного ИИ (150 разборов в сутки на всех — меньше четырёх мыслей в день на сорока платящих); радар перестал быть отдельным продуктом и строится слоем внутри MyBrain.
**Самое ценное — раздел противоречий.** Три главных: 1,5 млн ₽ на странице (частный займ под 15–20% с возвратом из сервисов) и 1,5 млн ₽ в стратегическом репозитории (инвестиционный раунд в MyBrain, конвертируемый заём, 5–15% компании) — это две разные сделки с одинаковой суммой; SMM Engine и заказная разработка AI-агентов, на которых держится вся модель возврата, в стратегическом репозитории не описаны вовсе; AURA на странице стоит вне модели возврата, а в стратегических документах названа «кислородом», финансирующим ИИ и бету MyBrain.
**Чувствительное вырезано** — репозиторий публичный: личный опыт фаундера, проекты третьих лиц, поведенческий паттерн из карты личности, конкурентный ров и реестр рисков AURA, неотобранные идеи из накопителя, внутренние оценки конкурентов. Помечено «[раскрывается устно]», список для проверки — в конце брифа.

### Сверка со стратегией: портфель источников вместо фальшивой точности (август 2026)
Стратегический бриф веб-Claude вскрыл расхождения страницы и стратегии. Решение Михаила: «откуда придут первые деньги — честно неизвестно, не выдумывать». Черновик перестроен: раздел 02 — «Четыре источника возврата» (заказные проекты · SMM-подписка · AURA · бета MyBrain) с честной оговоркой «какое направление принесёт первые деньги — покажут первые месяцы» и честной арифметикой беты (₽39,6k/мес долг не закрывает). Фактология обновлена по стратегии: MyBrain работает в проде (статус «строится» в лаборатории, лид «две работают, ещё две строятся»), бета 40×990 ₽ навсегда, Meridian — инфраструктура рыночной правды, вырастает из ядра MyBrain (стрелка на карте перерисована, SMM Engine остался без стрелки эволюции).
Открытыми остаются (для решения Михаила позже): природа суммы (займ студии vs конвертируемый раунд MyBrain из стратегического репо — две разные сделки), судьба помесячного плана М4–М12 в терминале (фальшивая точность), проксирование mybrain.rovensky.ru (недоступен из РФ).

### Калибровка статусов по Михаилу (август 2026)
«Сейчас нет работающих продуктов. Есть MyBrain, который тестируется». Статусы схемы лаборатории пересобраны: Инженерия и Исследования — «ЕЖЕДНЕВНАЯ ПРАКТИКА» (факт метода, не заявка о продукте), Редакция — «СТРОИТСЯ», Операции/MyBrain — «ТЕСТИРУЕТСЯ» (без ghost-пунктира: реальнее проекта), Сделки/AURA — «ПРОЕКТ». Лид: «Продуктов в продаже пока нет: MyBrain тестируется, остальное строится». Формулировка «работает в проде» удалена со страницы всюду (карточка, карта, трекшн, источники возврата) — заменена на «собран и тестируется».

### Панель ревью черновика и большая чистка (август 2026)
По запросу Михаила («собери всё и прогони через экспертов») черновик прогнан через 5 линз: кредитный аналитик, холодное чтение 45+, логика цифр, редактор, арт-директор (отчёты — `audit/next-2026-08/`). Внесено ~40 правок одним заходом, ключевое:
— Рамка возврата честная: «Возврат: два источника в модели — и два сверх» (было противоречие «четыре источника» ↔ «два направления» в пяти местах); карточки размечены «В модели / Сверх модели».
— Обложка: «возврат — к М18 · по плану выручка перекрывает долг к М11» (было «возврат по плану — М11» — читалось как обещание ранних денег).
— Точность → сценарий: таймлайн «М3–М5 первая выручка», «плановый сценарий, не прогноз»; контрольная точка «подписаны 2 пилота, оплаты — М4».
— Единый словарь статусов по всем схемам (тестируется/строится/проект); AURA «платится с первой продажи».
— Вариант Б: формула зачёта = сумма займа с процентами, поддержка в пакете.
— Резерв больше не страхует три риска сразу; рост API — статья «Нейросети».
— Структура: факт-полоса (Амодеи/Medvi/Klarna) слита с цитатами в манифесте; блок «Траектория студии» удалён (третий пересказ трёх горизонтов), роли ступеней — легендой под картой; полоса долей над сметой; смета с расшифровкой железа и ₽83k/мес.
— Язык: bus factor, митигация, классифайды, Seed-раунд — заменены; «пятая точка» → «пятая звезда»; терминал переименован в «пульт займодавца» (производственная метафора едина).
Объём: 3320 слов (было 3421; боевая 3032).
**Нерешённое — на Михаиле:** (1) перевод модели возврата с выручки на свободный остаток (мини-P&L, нужны цифры расходов М7–М18); (2) судьба оргсхемы vs карта (арт-директор: конкурируют, предлагает сжать оргсхему или слить); (3) позиционная расшифровка оборудования 464,5k; (4) фото в раздел 08; (5) сокращение акта I вдвое (холодное чтение) — частично сделано, можно жёстче.

### Концепт D «Демонстрация» после отказа от A/B/C (август 2026)
Михаил отверг все три дизайн-направления («ничего не нравится, не цепляет»). Диагноз: все три были статичными «обложками» — палитра+типографика, без драматургии. Четвёртый заход — принципиально другой род: страница ДОКАЗЫВАЕТ тезис «работу делают системы» собственным поведением.
— Пролог-кинотитры: при первом открытии лаборатория на глазах «собирает» страницу — терминальный лог (Инженерия/Исследования/Редакция/Операции отчитываются, «выпуск подтверждён архитектором»), затем экран уходит вверх и заголовок выезжает из маски. Пропускается кликом/клавишей/кнопкой, показывается один раз за сессию (sessionStorage), при prefers-reduced-motion не показывается вовсе.
— Машина «печатает» строку сделки под лидом (займ · ставка · срок · возврат) с курсором.
— Манифест: слова проявляются чернилами по мере скролла (scroll-driven ink) — приём уровня Apple/Linear.
— Цифры факт-блока крутятся слот-машиной при появлении.
— Палитра: глубокий чернильно-зелёный (#0A0C0B) + кость (#ECE9E1) + фосфор терминала (#49E29B) — цвет привязан к метафоре лаборатории, а не декоративен. Playfair огромным кеглем, курсивное слово — фосфором.
Технические грабли: (1) `.wrap` с `margin:0 auto` внутри flex-колонки схлопывался до контента — добавлен `width:100%`; (2) `padding:26px 0` у `.hero-top`/`.hero-mid` перебивал боковые отступы `.wrap` — переведены на padding-top/bottom.
Live: https://rovensky.ru/concepts/d/ . Решение за Михаилом; у него же запрошены референсы вкуса (что лично цепляет), если и D мимо.

## Текущее состояние
- Черновик новой версии: https://rovensky.ru/next/ — рабочая площадка, публикуется отдельно.
- Основная страница: https://rovensky.ru/pre-seed/ — открывается из РФ без VPN (мобильный интернет проверен), готова к отправке адресатам.
- Персонализация: `?to=Имя` → «Подготовлено лично для: Имя».
- Архивы: `/pre-seed/v1/` (первая версия), `/pre-seed/v2/` → редирект.
- Артефакты аудита: `audit/pre-seed-2026-07/` (критика, вердикт, бэклог, речь).

## Открытые пункты
1. Активация FormSubmit после восстановления их сервиса (тестовая отправка → клик в письме → контрольная отправка).
2. Фото Михаила для раздела «Кому вы даёте деньги».
3. Письмо о намерениях от собственника портфеля ₽100 млн — сильнейшее возможное усиление.
4. Решения из бэклога корзины B перед серьёзными переговорами: фикс ставки, график выплат, статус ИП, страхование жизни.
5. `mybrain.rovensky.ru` — проксируется Cloudflare и хостится на Vercel: проверить доступность из РФ, при необходимости перенести.
